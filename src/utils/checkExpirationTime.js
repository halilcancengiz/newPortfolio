// import { logout } from "../services/firebase/firebase";

// export const checkExpirationTime = (user) => {
//     const date = new Date()
//     const expirationTimeInMs = user ? new Date(user.expirationTime) : null;

//     if (
//         expirationTimeInMs !== null &&
//         expirationTimeInMs.getTime() < date.getTime()
//     )
// }